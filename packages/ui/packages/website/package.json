{
  "name": "@webjsdev/ui-website",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "imports": {
    "#*": "./*"
  },
  "description": "Redirect-only host for ui.webjs.dev. The component gallery and the registry API are served by the marketing site at webjs.dev/ui.",
  "scripts": {
    "dev": "webjs dev --port ${PORT:-5003}",
    "start": "webjs start"
  },
  "dependencies": {
    "@webjsdev/cli": "^0.10.0",
    "@webjsdev/core": "^0.7.0",
    "@webjsdev/server": "^0.8.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
