# The origin this host redirects to. Defaults to https://webjs.dev when unset,
# which is what the deployed service uses; override it for local dev so the
# redirects point at your local marketing app instead of production.
SITE_URL=http://localhost:5001
